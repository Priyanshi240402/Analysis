{
  "version": 2,
  "builds": [
    {
      "src": "youtube.py",
      "use": "@vercel/python",
      "config": { "maxDuration": 30 }
    }
  ],
  "routes": [
    { "src": "/fetch_and_store/([^/]+)", "dest": "/youtube.py" },
    { "src": "/(.*)", "dest": "/youtube.py" }
  ]
}
